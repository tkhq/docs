---
title: "React"
sidebar_position: 3
description: React SDK
slug: /sdks/react
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import CodeBlock from "@theme/CodeBlock";
import Parameter from "@site/src/components/parameter";

## 1. Installation
<Tabs>
  <TabItem value="yarn" label="Yarn" default>
    <CodeBlock language="bash">{`yarn add @turnkey/sdk-react`}</CodeBlock>
  </TabItem>
  <TabItem value="npm" label="NPM">
    <CodeBlock language="bash">{`npm install @turnkey/sdk-react`}</CodeBlock>
  </TabItem>
</Tabs>

## Initializing

In `App.tsx` (or equivalent file)
<CodeBlock language="typescript">
{`import { TurnkeyProvider } from "@turnkey/sdk-react";`}
{`\nimport turnkeyConfig from "./turnkey.json";`}
{`\n\n<div className="App">
  <TurnkeyProvider config={turnkeyConfig}>
    // Rest of app ...
  </TurnkeyProvider>
</div>`}
</CodeBlock>

## Using Turnkey

In any react component nested under the `TurnkeyProvider`

<CodeBlock language="typescript">
{`import { useTurnkey } from "@turnkey/sdk-react";`}
{`\nconst { turnkey, passkeyClient, iframeClient } = useTurnkey();`}
{`\n\nconst loginWithPasskey = async () => {
  await passkeyClient?.login();
}`}
{`\n\nconst initEmailAuth = async () => {
  await turnkey?.serverSign(
    "emailAuth",
    [{
      email: "<target user email>",
      targetPublicKey: \`\${iframeClient.iframePublicKey}\`,
      organizationId: "<target user suborg-id>"
    }]
  )
}`}
{`\n\nconst loginWithIframe = async (credentialBundle: string) => {
  await iframeClient?.injectCredentialBundle(credentialBundle);
  await iframeClient?.login();
}`}
</CodeBlock>
