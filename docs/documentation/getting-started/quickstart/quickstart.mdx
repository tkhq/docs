---
id: index
title: "Quickstart"
sidebar_position: 1
description: Get started with Turnkey by creating an organization and obtaining your keys.
slug: /getting-started/quickstart
---

import DocCardList from "@theme/DocCardList";

import { Step, Steps } from "@site/src/components/step";

# Quickstart

Before diving into the code, let's set up your organization and conjure up an API keypair to unlock the full potential of Turnkey!

## Create an Account

Navigate to the [Turnkey Dashboard](https://app.turnkey.com/dashboard/auth/initial) to create an account and setup your organization:

<p style={{ textAlign: "center" }}>
  <img
    src="/img/quickstart/create_organization.png"
    alt="Create organization"
    style={{ width: 800, borderRadius: "4px", boxShadow: "0 2px 5px 0 #0003" }}
  />
</p>

## Get Your Organization ID

Once logged in, open the user dropdown at the top right. Your Organization ID is listed there. Copy it for use in your code or environment variables.

<p style={{ textAlign: "center" }}>
  <img
    src="/img/quickstart/org-id.png"
    alt="Organization ID"
    style={{ width: 800, borderRadius: "4px", boxShadow: "0 2px 5px 0 #0003" }}
  />
</p>

## Create an API Key

The API keypair is used to authenticate requests to Turnkey. We'll create one now.

<Steps>
  <Step>Navigate to the User Details page.</Step>

<img
  src="/img/quickstart/user-details.png"
  alt="User Details"
  style={{ width: 800, borderRadius: "4px", boxShadow: "0 2px 5px 0 #0003" }}
/>

<Step>Click "Create an API key".</Step>

<img
  src="/img/quickstart/create-api-key.png"
  alt="Create API key"
  style={{ width: 800, borderRadius: "4px", boxShadow: "0 2px 5px 0 #0003" }}
/>

<Step>Choose a key generation method</Step>

For this guide, we'll use the in-browser method.

<img
  src="/img/quickstart/generate-api-key.png"
  alt="Generate API key"
  style={{ width: 800, borderRadius: "4px", boxShadow: "0 2px 5px 0 #0003" }}
/>

<small style={{ background: "grey", }}>

Optionally, you may also generate keys using the <a href="/sdks/cli">Turnkey CLI</a>.

</small>

<Step>Name your keypair</Step>

<img
  src="/img/quickstart/name-api-key.png"
  alt="Name API key"
  style={{ width: 800, borderRadius: "4px", boxShadow: "0 2px 5px 0 #0003" }}
/>

<Step>Approve & Create</Step>

You'll be prompted to authenticate with the authenticator setup during account creation.
Save the private key in a secure location — **it won't be visible after this step**.

**Important**: Both the public and private keys are required for signing requests to the Turnkey API. Keep these keys secure and out of reach of end-users.

<img
  src="/img/quickstart/approve-create-api-key.png"
  alt="Approve API key"
  style={{ width: 800, borderRadius: "4px", boxShadow: "0 2px 5px 0 #0003" }}
/>

<Step>API Key Created 🎉</Step>

<img
  src="/img/quickstart/api-key-created.png"
  alt="API key created"
  style={{ width: 800, borderRadius: "4px", boxShadow: "0 2px 5px 0 #0003" }}
/>

</Steps>

## Next Steps

Now that you've created an organization and API keypair, you're ready to start developing with Turnkey!

<DocCardList
  items={[
    {
      type: "link",
      href: "/getting-started/embedded-wallet-quickstart",
      label: "Embedded Wallet",
      description: "Build Embedded Wallets with Turnkey",
    },
    {
      type: "link",
      href: "/getting-started/signing-automation-quickstart",
      label: "Signing Automation",
      description: "Automate signing with Turnkey.",
    },
  ]}
/>
